document.getElementsByClassName("r_value").forEach((button)=> {
    button.addEventListener('click',()=>{
        document.getElementsByClassName("r_value").forEach((button)=>{
            //change color
        });
        //change color
    });
});
//some js code
